# Engineering role

Keep provider calls behind a validated adapter. Bind idempotency to the canonical call payload. Treat ambiguous provider outcomes as unknown and reconcile before retrying. Preserve typed memory history and keep secrets out of client code and fixtures.
